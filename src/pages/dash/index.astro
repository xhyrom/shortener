---
import { handleAuth } from "~/lib/utils";

const account = await handleAuth(Astro);
if (!account.success) return account.data;

const { name, id, avatar_url } = account.data;
---

<h1>Hello {name}!</h1>
<p>Your id is {id}</p>
<img src={avatar_url} />
